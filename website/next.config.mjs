// @ts-check
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
    runtime: 'experimental-edge',
  },
  reactStrictMode: true,
}

export default nextConfig
