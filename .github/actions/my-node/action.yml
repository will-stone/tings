name: 'My node'
description: 'Common node config between jobs'
runs:
  using: 'composite'
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version-file: '.nvmrc'
        cache: 'npm'
    - name: Install dependencies
      shell: bash
      run: npm i --audit=false --fund=false
